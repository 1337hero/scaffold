# 2026-07-03 — PRD 06: Fact system

**Task:** Implement GitHub issue #24 (PRD 06, `specs/v2-prds/06-fact-system.md`) — entity-keyed fact store with trust scoring, fact_edges, and relational query API.

**Shipped:** branch `feature/prd-06-facts`, PR #46 (https://github.com/1337hero/scaffold/pull/46, closes #24).

- `daemon/db/facts.go` — Fact/FactEdge types; InsertFact (trims entity, defaults trust 0.5, validates category enum, creates 'about' edges in same tx from related_entities); GetFact/UpdateFact/SuppressFact/ListFacts; ProbeFacts (floor + bump retrieval_count in tx); ReasonFacts (AND of entity-or-edge match per entity); RelatedEntities (CTE: all entities sharing a fact that touches the target); ContradictingFacts (same-entity surface, semantic detection deferred per PRD); FeedbackFact (single UPDATE, MIN/MAX clamp, +0.05/-0.10, helpful bumps helpful_count); escapeLike helper.
- `daemon/api/handlers_facts.go` + `server.go` — 10 protected routes: GET/POST /api/facts, GET/PATCH/DELETE /api/facts/{id}, GET probe/reason/related/contradict, POST /api/facts/{id}/feedback.
- `daemon/db/facts_test.go` — 22 tests, covers every name in the PRD Testing Decisions section.

**Judgment calls (ratify in PR review):**
- Trust floor 0.3 applies to agent retrieval ops only; ListFacts excludes suppressed but not low-trust — quarantine you can't inspect is deletion (story 24: Mike browses everything).
- Story 17 (top-N for prompt injection) = ListFacts `trust DESC` + limit param, no separate function.
- Feedback returns the updated fact (200) so callers see new trust without a second call.

**Review:** fresh-context go-reviewer on the diff found two real bugs, both fixed with regression tests: untrimmed entity keys orphan facts from exact-match queries; tag LIKE filter needed wildcard escaping (`a_b` matched `axb`).

**Verified:** `go build ./...`, `go vet ./...`, `go test ./...` green; gofmt clean on new files; standards greps (no raw SQL in api/, `make([]T,0)` slices) pass.

**Note:** PR #45 (PRD 05 Projects) was merged to main before this session started.
