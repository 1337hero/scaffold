# 2026-07-02 — PR #39 review + v1 daemon Gmail incident

**Task / shape:** review (PR #39, PRD 01 demolition, closes Issue #21) + investigation (rogue email triage).

## Review — PR #39 (`prd/01-demolition` → `v2`)

Method: own pass + go-reviewer subagent over the full 18.8k-line diff, ponytail lens.
Verdict: **right direction, incomplete cut** — callers deleted, ~900+ lines of corpse code/config/schema left compiled in.

Blockers:
1. **Auth bypass** (pre-existing from 80eefb5, on `v2` base too): `api/server.go:255` `authorizedByCookie` → `return true, nil`; same in `api/auth.go` `handleAuthCheck`. Every protected route open; `go vet` fails on the unreachable code. Must not merge over it.
2. **Goals half-deleted:** routes removed from `server.go` but `handlers_goals.go` (196 lines dead), `db/goals.go` (228), cortex bulletin Goals section, and frontend `queries.js:118-131` calls all remain → goals UI 404s. v2 spec has no goals (projects replace) → finish the deletion.

Should-fix: dead triage subsystem in `brain/triage.go` (~180 lines, zero callers) with `main.go` fatally binding dead `brain.triage`/`brain.prioritize` LLM routes; `daemon/webhook/` package fully orphaned; `db/db.go` still creates all v1 corpse tables (captures, desk, ingestion_*, gmail_waiting_threads, memory_embeddings, embedding_jobs, memory_centrality) on fresh boot; `config.go` hard-requires `embedding.yaml` + injects handler-less prioritize/session_cleanup cortex defaults (startup log spam); `tools.yaml` ships lying `search_email`/`get_email` descriptions over "not available" stubs; `config/webhooks.go` orphaned.

Nits: orphaned session-bus comment `main.go:512`; default prompt says "inbox"; `legacy_types.go` dead score fields; STATE.md duplicated table (fixed this session); `dumpResponse`/`DumpMemories` unused; untracked ELF binaries.

**Verification ledger:** `go build ./...` ✅, `go test ./...` ✅ (7 pkgs), `go vet ./...` ❌ (auth bypass unreachable code — PR body's verification claim omitted vet). Skipped: frontend build (no frontend changes in PR).

## Incident — v1 daemon triaged Mike's Gmail today

- `daemon/bin/scaffold-daemon` built 14:14 local from **pre-demolition** code (demolition commit landed 20:13); symbols confirm gmail_triage/session-bus present.
- Ran ~13:56–14:12: labeled thread "Your payment is scheduled for 07/01/2026" WAITING (the triage Mike saw), captured Tony Bomkamp prison-schedule email + $3,104.74 1337 Hero LLC withdrawal notice as `do` captures.
- Now: no processes, systemd units inactive + linked-only (no autostart). **Deleted the stale binary** (derived decision — it was the recurrence hazard; rebuild is trivial). Rebuild from branch failed → working tree is mid-sweep and doesn't compile, so no runnable binary exists tonight (safe state).
- `scaffold.db` still holds a live Google OAuth token (gmail scope). v2 goes fresh-DB anyway; consider revoking the token's Gmail scope.

## Concurrent sweep (not this session's edit)

Uncommitted working-tree changes (~−1,662 lines) applying the review findings appeared mid-session: goals/webhook/webhooks.go/dashboard deletions, triage strip, db.go schema cleanup, auth.go touch, llm.yaml/tools.yaml trims, stray binaries removed. **Broken mid-edit:** `brain/triage.go:146-159` references deleted `fallbackTriage` + unused `scaffold/llm` import. Left untouched to avoid collision.

## Derived decisions (ratify in PR?)

- Deleted `daemon/bin/scaffold-daemon` (stale v1 build artifact, untracked) without asking — it was actively dangerous.
- Updated agent memory: GitHub Issues now enabled by Mike as PRD mirrors; `specs/` stays canonical.
- Fixed STATE.md duplicated "Key files / commands" table.

## Open / next

1. Finish the in-flight sweep (compile fix in `brain/triage.go`), rebuild, build+vet+test green, land on PR #39.
2. Verify auth bypass removal in the sweep before merging #39.
3. Merge #39 → `v2`, then PRD 02 (LLM config rework).
