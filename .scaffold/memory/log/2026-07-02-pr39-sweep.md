# 2026-07-02 (late) — PR #39 review sweep completed

**Task / shape:** fix — take over and finish the half-done review sweep on `prd/01-demolition`, close all PR #39 review findings.

## Changed

Commit `bae3308` (38 files, −2,758/+304), pushed, summarized in PR #39 comment. Inherited a mid-flight uncommitted sweep from a concurrent session (auth bypass removal, goals/dashboard/webhook deletions, triage strip, db.go schema rewrite — good but non-compiling); completed it:

- `config/llm.go`: deleted dead route constants + default injection for triage/prioritize/semantic/observations (root cause of config test failures)
- `config/config.go`: deleted `TriageConfig`, `EmbeddingConfig`, their loads/defaults/validation; `config/embedding.yaml` removed
- `config/cortex.yaml`: stripped 9 handler-less tasks (kept bulletin/consolidation/decay/prune)
- `brain/triage.go` → `brain/brain.go`: removed dead Brain/Config fields, orphaned helpers (`sanitizeTriageJSON`, `extractFirstJSONObject`, `fallbackTitle`), `completion_types.go` `Dependencies` struct
- Task layer: stripped `source`/`source_ref` (webhook provenance) from struct/queries/API/tool; deleted `TaskBySourceRef`
- `db/db.go`: dropped `gmail_waiting_threads`; **fixed two fresh-DB breaks the rewrite exposed** — missing `notes.task_id` column (live feature: create_note links to tasks) and `idx_tasks_source_ref` on a dropped column
- `db/notifications.go`: `NotifiableTasks` joined the deleted `goals` table — would have failed at runtime on fresh DB; join removed
- Deleted `ReindexMemoryCentrality`/`MemoryCentralityCount`/`ReindexReport`, `DumpMemories`, `dumpResponse`, ScoredMemory vector/fused fields
- `llm/runtime.go`: `requirementForRoute` cases for dead routes removed
- Tests updated: config_test (8 tools, no triage/prioritize/embedding expectations), llm_test, runtime_test (dead route constants → cortex.bulletin)
- Local untracked `config/gmail.yaml` + `config/webhooks.yaml` deleted

## Verification ledger

- `go build ./...` ✅  `go vet ./...` ✅ (auth bypass unreachable-code failure gone)  `go test ./...` ✅
- Fresh-DB migrate exercised by google package tests (was RED on `source_ref` index before the schema fix — red-for-the-right-reason, then green)
- Rebuilt `daemon/bin/scaffold-daemon`: `strings` shows zero `gmail_triage`/session-bus symbols (v1-daemon incident can't recur from this artifact)
- Skipped: frontend build (app/ untouched; v1 UI already incompatible, Phase 3 rebuilds it)

## Derived decisions (ratify in PR)

- Finished the goals deletion rather than restoring routes — v2 spec replaces goals with projects; kept `goal_id` columns as inert TEXT until Phase 2 renames the world
- Kept `notes.task_id` (live create_note feature) — restored the column the schema rewrite dropped
- Cut `source`/`source_ref` through the whole task layer instead of re-adding schema columns — webhook provenance has no v2 writer
- Left `app/src` goals/dashboard calls for Phase 3

## Open / next

1. Merge PR #39 → `v2`
2. PRD 02: LLM config rework (llama.cpp provider)
3. Revoke Gmail scope on the OAuth token in `scaffold.db`
